function foo(a,b){return a+b;}console.log(foo(1,2));//3console.log(foo(1,"2"));//12console.log(foo([1,2],[3,4]));//[1,2][3,4]console.log(foo({a:1},{b:2}));//[object Object][object Object]In JavaScript, the + operator performs both addition and string concatenation. When adding a number and a string, JavaScript converts the number to a string and performs string concatenation. When adding two arrays or objects, the + operator concatenates them as strings.